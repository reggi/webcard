# Webcard Format Changelog

## 1.0.0

Initial public specification.

The release defines a constrained ZIP container, `webcard.json`, directly named capture JSON documents, content addressed typed assets, namespaced extensions, SHA-256 integrity, secure processing requirements, and compatibility rules. Integer archive versions `1` and `2` are unsupported private prototypes and are not public specification versions.
